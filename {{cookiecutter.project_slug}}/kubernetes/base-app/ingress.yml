apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ cookiecutter.dns_slug }}-ingress
  labels:
    app: {{ cookiecutter.dns_slug }}
  annotations:
spec:
  rules:
  - host: {{ cookiecutter.dev_domain }}
    http:
      paths:
      - path: /{{ cookiecutter.api_prefix }}
        backend:
          serviceName: {{ cookiecutter.dns_slug }}
          servicePort: 5000
