apiVersion: v1
kind: Service
metadata:
  name: {{ cookiecutter.project_slug }}
  labels:
    app: {{ cookiecutter.project_slug }}
spec:
  selector:
    app: {{ cookiecutter.project_slug }}
  ports:
  - port: 5000
    targetPort: 5000
